# A deliberately incomplete roles file (cli-tour demo) — declares `shopper` but NOT `merchant`,
# so resolving the storefront workspace against it surfaces the undefined-role gate.
roles:
  - name: shopper
    description: a customer placing and tracking orders
    accessRights:
      - DOCUMENT_CREATE
      - QUERY
